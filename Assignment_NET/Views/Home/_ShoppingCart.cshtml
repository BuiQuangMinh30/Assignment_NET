<ul class="nav navbar-nav  navbar-right">
        <li>
            @if (Session["CartCounter"] == null)
            {
                @Html.ActionLink("Cart", "ShoppingCart", "Home", new { id = "cartItem" })
            }
            else
            {
                @Html.ActionLink("Cart("+Session["CartCounter"]+")", "ShoppingCart", "Home", new { id = "cartItem" })
            }
        </li>
        <li></li>
    </ul>
@*<ul class="nav navbar-nav  navbar-right">
    <li>
        @if (Session["giohang"] == null)
        {
            @Html.ActionLink("Cart", "ShoppingCart", "BagCart", new { id = "cartItem" })
        }
        else
        {
            @Html.ActionLink("Cart(" + Session["CartCounter"] + ")", "ShoppingCart", "BagCart", new { id = "cartItem" })
        }
    </li>
    <li></li>
</ul>*@
